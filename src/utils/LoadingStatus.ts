// named export
// eslint-disable-next-line import/prefer-default-export
export enum LoadingStatus {
    Idle = 'idle',
    Loading = 'loading',
    // Succeeded = 'succeeded',
    Failed = 'failed',
}
